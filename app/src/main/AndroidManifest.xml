<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.meiadois.decole">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.user.RouteDetailsActivity"/>
        <activity android:name=".activity.user.HomeActivity" />
        <activity android:name=".activity.instagram.HasInstagramAccountActivity" />
        <activity android:name=".activity.instagram.InstagramIntroActivity" />
        <activity android:name=".activity.welcome.WelcomeSlideActivity" />
        <activity android:name=".activity.welcome.WelcomeInfoActivity" />
        <activity android:name=".activity.AskForInteractiveModePermissionActivity" />
        <activity android:name=".activity.welcome.PasswordDefinitionActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".service.FloatingViewService" />
    </application>

</manifest>